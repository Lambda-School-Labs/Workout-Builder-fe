import React from "react";

export default function AddExercise(props) {
  const {id} = props;
  return (
    <div>
      <p>{`This is a temp exercise EDIT page and the id is: ${id} `}</p>
    </div>
  );

}